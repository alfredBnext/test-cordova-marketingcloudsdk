import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'test-cordova-marketingcloudsdk',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
